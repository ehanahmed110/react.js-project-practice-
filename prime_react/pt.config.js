export const ptOptions = {
  inputtext: {
    root: {
      className: "w-full text-sm border !rounded-[0px]  px-4 focus:ring-2 focus:ring-blue-500 mb-2 !py-1"
    }
  },
  dropdown: {
    root: {
      className: "w-full text-sm border !rounded-none focus:ring-2 focus:ring-blue-500 mb-2 !h-[34px] flex items-center"
    },
     label: {
      className: "!py-0 !leading-none"  // Match InputText padding
    },
     trigger: {
      className: "!py-0 px-2"
    }
  },

  // Add more components here if needed
};